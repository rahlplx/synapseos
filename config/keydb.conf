# KeyDB — ARM tuned
server-threads 2
maxmemory 1536mb
maxmemory-policy allkeys-lru
# AOF only — RDB fork() causes OOM on ARM
appendonly yes
appendfsync everysec
no-appendfsync-on-rewrite yes
save ""
bind 0.0.0.0
port 6379
tcp-keepalive 300
