# ─── SynapseOS Environment Variables ───────────────────────────────────────
# Copy this file to .env and fill in your values.
# NEVER commit .env to Git.

# ─── Database ──────────────────────────────────────────────────────────────
POSTGRES_PASSWORD=change-me-to-a-strong-random-string

# ─── MinIO (S3-compatible object storage) ──────────────────────────────────
MINIO_USER=synapseos
MINIO_PASSWORD=change-me-to-a-strong-random-string
MINIO_ACCESS_KEY=${MINIO_USER}
MINIO_SECRET_KEY=${MINIO_PASSWORD}
MINIO_ENDPOINT=minio:9000

# ─── Encryption (BYOK key encryption at rest) ─────────────────────────────
# Generate: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=change-me-to-base64-32-byte-key

# ─── Langfuse (self-hosted observability) ──────────────────────────────────
# Generate both with: openssl rand -base64 32
LANGFUSE_NEXTAUTH_SECRET=change-me-to-random-32-chars
LANGFUSE_SALT=change-me-to-random-32-chars
LANGFUSE_PUBLIC_KEY=pk-lf-change-me
LANGFUSE_SECRET_KEY=sk-lf-change-me

# ─── LLM Provider Keys (platform defaults — tenants override via BYOK) ────
# Get free Groq key: https://console.groq.com
GROQ_API_KEY=gsk_change-me
# Get OpenRouter key: https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-change-me
# Get Anthropic key: https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-change-me

# ─── MCP Service Key ──────────────────────────────────────────────────────
MCP_SERVICE_KEY=sk-syn-mcp-change-me

# ─── Docling (CPU-only mode for ARM / resource-constrained environments) ───
DOCLING_CPU_ONLY=1

# ─── Application URLs (Docker-internal) ───────────────────────────────────
DATABASE_URL=postgresql+asyncpg://synapse:${POSTGRES_PASSWORD}@postgres:5432/synapseos
QDRANT_URL=http://qdrant:6333
KEYDB_URL=redis://keydb:6379
LITELLM_URL=http://litellm:4000
LANGFUSE_HOST=http://langfuse:3100
